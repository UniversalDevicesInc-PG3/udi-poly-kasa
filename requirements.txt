udi_interface >= 3.3.14
deprecation
# Pip fallback only; install.sh clones jimboca tpap-gmpy2-mpz-fix for TPAP.
python-kasa >= 0.10.0, < 0.11.0
mashumaro
async_timeout
# Required by nested python-kasa TPAP transport (kasa/transports/tpaptransport.py).
# FreeBSD: if pip cannot install these, use:
#   pkg install py311-ecdsa py311-passlib
ecdsa>=0.19.1
passlib>=1.7.4
